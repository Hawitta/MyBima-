<%= form_with(model: user_payment) do |form| %>
  <% if user_payment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_payment.errors.count, "error") %> prohibited this insurance_company from being saved:</h2>

      <ul>
        <% user_payment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<!-- <div>
  <%= form.label :user_id, style: "display: none" %>
  <%= text_field_tag :user_id, session[:user_id] %>
</div> -->

<%= form.hidden_field :user_id, value: session[:user_id] %>

  <div>
    <%= form.label :cover_id, style: "display: inline" %>:
    
     <%= @selected_cover.cover_name %>
  </div>

  <div>
    <%= form.label :amount_paid, style: "display: block" %>
    <%= form.text_field :amount_paid %>
  </div>

  <div>
    <%= form.label :payment_mode, style: "display: block" %>
    <%= form.text_field :payment_mode %>
  </div>

  <br/>
  <div>
    <%= form.submit%>
  </div>

<% end %>
